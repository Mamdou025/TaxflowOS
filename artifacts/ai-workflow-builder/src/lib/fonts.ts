// In Vite, fonts are loaded via Google Fonts link tag in index.html
// These CSS variable names match the original Next.js font config
export const sans = {
  variable: '--font-geist-sans',
  className: '',
};

export const mono = {
  variable: '--font-geist-mono',
  className: '',
};
