# Workflow Builder — Audit Index

*Entry point for the living audit. All other files are linked below.*
*Last updated: 2026-06-07*

---

## Files in This Audit

| File | Covers |
|---|---|
| [ARCHITECTURE.md](./ARCHITECTURE.md) | Stack, layer model, execution model, state atoms, serialization, evidence/governance system |
| [BLOCKS.md](./BLOCKS.md) | Every block type — family, subtype, tool ID, config, inputs/outputs, allowed edges |
| [FEATURES.md](./FEATURES.md) | Full feature inventory with `[LIVE]` / `[PARTIAL]` / `[STUB]` / `[PLANNED]` status |
| [UI.md](./UI.md) | Every UI component — shell, canvas, nodes, inspector, viewers, overlays, AI elements |
| [TYPES.md](./TYPES.md) | All TypeScript types and domain model — blocks, edges, execution, evidence, lineage |

> **Target architecture (forward-looking, not current state):** [`docs/kernel-migration-spec.md`](../docs/kernel-migration-spec.md) — the product-kernel unification plan (one workflow/run model, action gateway, versioned agents, surfaces, artifacts) and the file-by-file migration mapping. The audit files above describe what exists **today**; the spec describes where it's going. **Step 1 (stabilize the vocabulary) landed 2026-07-17 — `tsc` is green; see the Global App Shell log + [TYPES.md](./TYPES.md).**

---

## App State Summary (2026-05-17)

### What this app is
A **fiscal workflow automation studio** — a visual, node-based editor for building data transformation and classification pipelines with evidence tracking, data lineage, and governance compliance. Domain-specific to fiscal/tax work (FAPI, FX rates, keyword-based income/expense classification).

### Stack at a glance
Next.js 15 (App Router) · React 19 · @xyflow/react v12 · Jotai v2 · Monaco Editor · Drizzle ORM · better-auth · Vercel AI SDK v5

### Architecture summary
Four layers: **Canvas** (React Flow UI) → **Domain** (typed workflow schema) → **Tool Registry** (block toolId lookup) → **Backend Runtime** (per-block `run.ts` functions). Execution runs fully in-browser (no server compute for block runs). State is managed via Jotai atoms. Persistence is localStorage for the local mode; Drizzle/Postgres for database mode (partially wired).

### Block system summary
- **7 block families:** Trigger · Source · Logic · Review/Validation · Field · Output · AI/Agent
- **11 registered tool modules** — only Source and Logic families have live execution
- **Source blocks** carry immutable evidence anchors (`SourceMetadata`)
- **Logic blocks** transform, classify, or aggregate — never overwrite source truth
- **Review/Validation, Field, Output** families are domain-defined but largely unimplemented
- **AI/Agent** blocks propose changes via the prompt panel — approval flow partially wired

### Features summary
- **[LIVE]:** Visual canvas editing, undo/redo, local workflow execution (Keyword Mapper, Calculation Engine, Hierarchy Aggregator, Currency Rate, Keyword Rules), evidence tracking, source trace, inspector panel, AI prompt generation, keyboard shortcuts, autosave, **2 starter templates** (FAPI Calculation, Roulement fiscal art. 85)
- **[LIVE]:** Open-ended **generative UI** (OpenUI) — free-form prompt → live UI composed from a registered component library (54 OpenUI built-ins + custom `TaxMetric`), streamed as OpenUI Lang. Distinct from CopilotKit's fixed-action renders. Now runs **inside the chat** via the **`generateUI`** action (renderUI bridge, 2026-07-16) as well as the standalone `/genui-lab`. Reliability: default model `gpt-4.1` (`GENUI_MODEL`) + first-line prose-sniff/retry. Still open: generated forms are display-only (no writeback), data still sample. See [FEATURES.md](./FEATURES.md) / [UI.md](./UI.md).
- **[PARTIAL]:** Excel upload (UI done, parsing WIP), Rollup/Calculation rule editors, workflow versioning types (no save logic), output mapping preview, publish flow
- **[STUB]:** PDF, API, DB, Web, AI Search source blocks, all Output block subtypes, Review/Validation blocks, AI gateway, integrations
- **[PLANNED]:** Full Review/Validation family, Field block live binding, Output handoff generation, version history UI

### Navigation architecture (2026-06-07)
- **Homepage:** `/` → `OrbitalStage` (neumorphic orbs + AI chat)
- **Worksheets hub:** a gallery of worksheet cards (FAPI · T1134 · Surplus · Executive Overview) reachable from the **Scope sidebar** ("All worksheets" → inline hub, opens worksheets inline) and the `GlobalTopNav` pill (→ `/worksheets` route). Shared `WorksheetsGallery`; inline hub = resource `worksheets` (added 2026-07-19). See UI.md → "Worksheets hub + menu".
- **Builder:** `/builder` → `WorkflowStudioShell` + `NodeConfigPanel` overlaid on `PersistentCanvas`
- **Legacy:** `/workflows` + `/workflows/[id]` → DB-backed workflow pages
- **ReactFlowProvider** moved from layout root → inside `PersistentCanvas` only; soft navigation (router.push) now works from all pages
- **CSS View Transitions** enabled via `@view-transition { navigation: auto }` — 180ms crossfade in Chrome/Safari

### Chat Workspace — Agentic Chat (added 2026-07-09)
The "bring all pages into the chatbot" direction: a **large de-modalized chat panel** (`components/workspace/chat-workspace-panel.tsx`) that unifies the conversation, the pages opened inside it, and scripted agent runs. The `ActionOrb` "AI Chat" radial item opens it. No click-outside-close — the conversation persists; only × minimizes it.
- **Thread is rich:** text bubbles with **entity chips** (EMAIL / FAPI-WORKFLOW / GROSS FAPI / SOPHIA), **`pageEvent`** cards (re-open a page), **`run`** timelines (agent runs), and **`result`** cards (inline generative UI, dismissible).
- **Agent run demo `[DISPLAY]`:** the FAPI flow — connect EMAIL → search → **checkpoint/approve** → run FAPI-WORKFLOW → compute GROSS FAPI. Steps reveal progressively, are expandable, and each can **Open** its page (e.g. FAPI-WORKFLOW → FAPI tab). Human-in-the-loop: the run pauses until the user approves. This is where the real governance gate will live.
- **Store:** `lib/workspace-store.ts` (windows + trail). `lib/chat-store.ts` adds `chatWorkspaceOpenAtom`, and `run` / `result` / `pageEvent` variants on `ChatMessage`. `components/workspace/entity-text.tsx` renders the chips.
- **Unified resource registry (2026-07-10):** `lib/resource-registry.tsx` is now the **single spine** — one `Resource` per named thing (integration/tool/workflow/value/agent/worksheet), each carrying every facet: chip (`token`/`mentions`), `open` behavior, `page` (lazy component/title/icon), `anchors` (deep-link targets), and `field` (inline-editable anchors). It replaces the four former registries — `chat-entities.ts`, `workspace-registry.tsx`, `workspace-targets.ts`, `fapi-model.ts` (all deleted). Resolvers: `splitMentions` (chips), `resolveIntent` (open/close/closeAll), `resolveTarget` (anchor focus), `resolveFieldEdit` (inline edit), plus `getPage`/`getFieldContext`. `fieldValuesAtom` (storage key `inscope.fapi.values`, unchanged) is the shared page↔chat value store.
- **Registered pages:** Dashboard · Executive Overview · FAPI · Surplus · T1134 · Client Workspace.
- **`ActionOrb`** slimmed to a launcher orb + radial nav (old inline chat bar removed).
- **Addressability (deep-link + focus):** the chat can bring a *specific piece* of a screen forward — `data-anchor="<page>:<id>"` on rows (data-derived) + a resource's `anchors[]`/`resolveTarget` (`lib/resource-registry.tsx`) + a rAF scroll-and-flash runner. Wired on **FAPI worksheet** (rows) and **Dashboard** (sections + client rows).
- **wouter SSR fix (2026-07-10):** the tax-ui route pages (`app/fapi`, `/dashboard`, `/t1134`, `/surplus`, `/bu-overview`, `/client/[id]`) now load via `dynamic(..., { ssr:false })` — previously `/fapi` and `/dashboard` 500'd on `location is not defined` from wouter's browser-only hooks. All routes now return 200.
- **Everything past the checkpoint is scripted/mock** (`[DISPLAY]`) — EMAIL and FAPI-WORKFLOW don't really execute yet.
- **LLM agent wired (2026-07-13):** `app/api/chat-workspace/route.ts` runs AI SDK v5 `generateText` with tools built from `buildAgentCatalog()` (the registry *is* the tool surface). The model returns tool-calls (`openPage`/`focusAnchor`/`editField`/`closePage`/`closeAll`/`openWorkflowBuilder`/`runFapiDemo`); the client (`lib/chat-agent.ts` → panel `executeAgent`) runs them. **LLM-first with a deterministic fallback** (`runDeterministic`) so the workspace still works with no key. Reuses the gateway model `openai/gpt-5.1-instant` + `AI_GATEWAY_API_KEY` (same as `/api/ai/generate`).
- **Run → resolve → resume loop wired (2026-07-13):** the scripted EMAIL timeline is **removed**; the FAPI run is now an interactive loop. `runFapiLoop(state)` (`lib/fapi-run.ts`) runs the real engine and returns the next **blocker** — `upload` (needs trial balance), `categorize` (a row matched no keyword rule → user assigns a category, injected as a keyword rule + re-run), or `approval` (final sign-off). The panel renders each as a `BlockerCard`; resolving accumulates into `FapiLoopState` and re-runs until done, then shows the result card + an inline editable FX field. Model key: `OPENAI_API_KEY` in `.env.local` (via `@ai-sdk/openai`).
- **Next steps:** route the approval blocker through `GovernanceMetadata.approvalState` (Step 2); worksheet **export** of the approved run; the agent auto-searching Gmail and auto-selecting the trial-balance attachment (today the user picks it); preserve page state across tab switches.

### Real Google Drive / Gmail source ingestion (2026-07-15)
Closes the ingestion link of the **agent → workflow → reviewed run → worksheet** spine: the FAPI trial balance can now be pulled from **real Google Drive or a real Gmail attachment**, not only a local-disk upload. Built on the OAuth tokens **better-auth already stores** (`accounts` table) — added read-only Drive+Gmail scopes to the Google provider (`lib/auth.ts`), a `server-only` token+REST layer (`lib/google/client.ts`, auto-refresh), thin routes under `app/api/google/*`, and a `GoogleSourcePicker` (`components/workspace/google-source-picker.tsx`) in the run's upload step. Key invariant preserved: routes return **raw bytes**, the client wraps them in a `File` and runs the **same `parseUploadToRows`** as a disk upload → identical rows/numbers across chat/builder/worksheet. Connect via `authClient.linkSocial` (works for anonymous sessions). Verified without creds: `/api/google/status` → `{connected:false, configured:false}` (no 500), data routes 401 without a session, the whole import chain compiles (home page 200). **To go live:** set `GOOGLE_CLIENT_ID/SECRET`, enable Drive + Gmail APIs, add the scopes to the consent screen. See ARCHITECTURE.md → "Google Drive / Gmail Ingestion".

### Global App Shell (updated 2026-07-10)
- **Document Viewer — a page to open PDF / Excel / Word in-app `[LIVE]` (2026-07-19):** a first-class page alongside the Workflow Builder / worksheets / Dashboard. `components/workspace/document-viewer.tsx` opens local files (upload + drag-and-drop) as closable tabs and renders each by type — **PDF** via a native `<iframe>` blob, **Excel** via an `xlsx` `sheet_to_html` grid (sheet switcher), **Word (.docx)** via client `mammoth`→HTML, text via `<pre>`. Two hand-offs make the files usable, not just viewable: **auto-context for Scope** — opening a doc automatically extracts its text into `attachedDocsAtom` (reusing the chat-attachment pipeline; Excel → client CSV) so the chat next to the viewer answers about it with no click (action bar shows a status chip; closing a tab removes it; failures leave a manual "Add to Scope" retry) — and **Load as workflow source**, which feeds a spreadsheet into `uploadedRowsAtom` for the FAPI run/builder. Registered as resource `viewer` (`lib/resource-registry.tsx`, page facet), routed at **`/viewer`** (`app/viewer/page.tsx`), and added to the top nav as **Documents** (`global-top-nav.tsx`). The `app/api/assistant/extract` route gained a `.docx` (mammoth) branch, so the chat's own attachments now read Word too. New dep: `mammoth`. Verified in a real browser (Playwright smoke: all three renderers mount, no errors, Discuss→toast) + server extraction (78-page PDF, 10.5k-char docx). See [FEATURES.md](./FEATURES.md) → "Document Viewer", [ARCHITECTURE.md](./ARCHITECTURE.md), [UI.md](./UI.md).
- **Chat attachments are actually read — PDF / text reach the model `[LIVE]` (2026-07-19):** fixes "why can't it see the document I attached?" The composer's `onAttach` (`components/assistant/use-assistant.tsx`) used to drop only a `[Attached <name>]` filename tag for anything that wasn't a workbook, so the model answered PDF questions from the **filename alone** and honestly said it couldn't see the content — a data-flow gap, **not** an OCR problem. Now non-workbook files are extracted server-side by a new **`app/api/assistant/extract`** route (Node runtime): PDFs via **`unpdf`** (embedded text layer, no OCR), plain-text decoded as UTF-8, normalized + capped at 120k chars with a `truncated` flag; a scanned/image-only PDF returns a clean **422 "needs OCR"**. The text lands in **`attachedDocsAtom`** (`lib/workspace-store.ts`, in-memory) and reaches the model through a new **attached-documents** `useCopilotReadable`; the message keeps only a short note. Mirrors the existing workbook→rows path. Verified end-to-end against the running dev server: `codedutravail.pdf` (78-page Senegal Labour Code) → `{pages:78, chars:120000, truncated:true}` with correct accents. Dep added: `unpdf`. See [ARCHITECTURE.md](./ARCHITECTURE.md) → "Chat attachments — document text reaches the model", [FEATURES.md](./FEATURES.md), [UI.md](./UI.md).
- **Ask/Propose/Execute intent gate — the chat stops running workflows on a mention `[LIVE]` (2026-07-18):** the reliability fix for "it starts a workflow when I only mention one / it misses a direct command." New **`lib/assistant-runtime/`** adds a deterministic intent layer wired into the existing `app/api/copilotkit/route.ts` `agent.use(...)` middleware (composed *before* orphan-repair): `computeGateDecision` classifies the current user turn into an `AssistantRoute` (`ask|propose|execute`) using bilingual EN/FR verb/question/negation/hypothetical detection (`command-parser.ts`) and a **separate** workflow-target resolver (`workflow-targets.ts`) — encoding *a workflow name is a target, never a command*. On ask/propose turns it **withholds `runWorkflow`** (+ builder-mutate tools) from `input.tools` so a mention/question can't trigger a run; on explicit commands it keeps them and appends an `[assistant-routing]` directive naming the resolved workflow. Pure + synchronous (the AG-UI middleware must return an Observable synchronously), **fail-open** (any doubt/error → unchanged), never strips the whole tool set. Controlled by **`ASSISTANT_INTENT_GATE`** = off/shadow/**enforce** (default); `INSTRUCTIONS()` also gained an INTENT section. **`pnpm assistant:evals`** runs the classifier + policy over **118 bilingual cases** + 4 gate checks offline → **118/118, 0 false executions, 0 missed commands, 16/16 negation/injection**. `tsc` clean for the slice (2 pre-existing errors remain in `lib/local-ai-workflow-assistant.ts`, untouched). Optional `classifyWithLLM` router built but out of the hot path. Rollback: `ASSISTANT_INTENT_GATE=off`. See [ARCHITECTURE.md](./ARCHITECTURE.md) → "Ask/Propose/Execute intent gate", [FEATURES.md](./FEATURES.md), [TYPES.md](./TYPES.md), `docs/assistant-routing-policy.md`.
- **Per-turn model tiering — smarter answers, cheaper navigation `[LIVE]` (2026-07-18):** the chat used ONE fixed model (`OPENAI_CHAT_MODEL`) for everything. `lib/assistant-runtime/model-policy.ts` now picks the model from the SAME route the gate computes — a **deep** model for hard tax/analysis questions (explain/inspect-calc/search/answer), a **fast** model for navigation (open/focus/edit-field), standard otherwise — via `BuiltInAgent` `overridableProperties: ['model','providerOptions']` + `forwardedProps.model` in `app/api/copilotkit/route.ts` (provider-prefixed spec required). **Safe by default: every tier defaults to `OPENAI_CHAT_MODEL`, so it's a NO-OP until `ASSISTANT_MODEL_FAST`/`DEEP` are set to distinct models** — then hard questions auto-upgrade. Reasoning-effort opt-in (`ASSISTANT_REASONING_ENABLED`, off). Rollback `ASSISTANT_MODEL_TIERING=off`. Evals green incl. tiering checks; tsc clean for the slice. See [ARCHITECTURE.md](./ARCHITECTURE.md) → "per-turn model tiering", `docs/assistant-model-policy.md`.
- **Specialists — "one assistant, many hats" `[LIVE]` (2026-07-18):** multiple agents done right — ONE conductor + ONE conversation, but each turn the assistant becomes the fitting domain specialist (Sofi=FAPI, Théo=roulement art.85, Mira=expense, Nova=campaign). Built on the intent layer: **`lib/assistant-runtime/agents/specialists.ts`** `selectSpecialist(route)` maps the route's resolved workflow → its specialist (general/nav → none). **Brain:** the `/api/copilotkit` middleware injects the persona+expertise as a model **context item** using the route the gate already computed (flag `ASSISTANT_SPECIALISTS`, default on); `INSTRUCTIONS()` got a SPECIALISTS section. **Face:** `components/assistant/specialist-presence.tsx` (`SpecialistPresence`, headless, mounted once in app-shell) reuses the same pure classifier client-side to light the `CoworkerActivity` indicator with the specialist while a reply streams — purely cosmetic (writes only the presence atom → can't break the chat), yields to a live run. NO Agents SDK — that's only needed for true handoffs/resumable approvals (a later step on this same base). Verified: `pnpm assistant:evals` incl. specialist-selection checks (workflow→specialist, general→none); tsc clean for the slice. Live persona + streaming indicator not verified here (needs the running app). See [ARCHITECTURE.md](./ARCHITECTURE.md) → "specialists", `docs/assistant-specialists.md`.
- **Durable assistant memory — the chat remembers facts across sessions `[LIVE]` (2026-07-18, needs `pnpm db:migrate`):** the chat kept everything in jotai+localStorage and never truly remembered. New **`lib/assistant-runtime/memory/`** + table **`assistant_memories`** (migration `drizzle/0005_strange_deathstrike.sql`) let the assistant durably save facts/preferences the user EXPLICITLY asks it to remember ("remember Acme's 2025 FX is 1.3978", "always CAD"), in Postgres, **per-user (tenant boundary) and scoped per client with no cross-client bleed** (pure, unit-tested retrieval policy: global-always, client-specific-only-for-its-client, globals-only when the client is unknown). `components/assistant/memory-copilot.tsx` (`MemoryCopilot`, headless, mounted once in `app-shell`) publishes remembered facts as grounding + registers **`rememberFact`/`forgetFact`**; `GET/POST/DELETE /api/assistant/memory` is better-auth-scoped and **fail-soft** (no session/DB → empty/structured error, chat unaffected). Explicit opt-in only (never auto-saved). `INSTRUCTIONS()` gained a MEMORY section. Verified: `pnpm assistant:evals` incl. memory-retrieval checks (scoping + no-bleed); `pnpm db:generate` produced the migration; tsc clean for the slice. **Live-DB round-trip (save→reload→recall) not verified here — run `pnpm db:migrate` against your Postgres to activate.** See [ARCHITECTURE.md](./ARCHITECTURE.md) → "durable assistant memory", `docs/assistant-memory-policy.md`.
- **One workflow, one set of numbers — three surfaces reconciled `[LIVE]` (2026-07-18):** fixed "the chat, the worksheet, and the result card show three different results for the same run." Causes + fixes in `components/workspace/workflow-run-flow.tsx`: (1) **`WorkflowElementCard` output** (the summoned "FAPI · OUTPUT" card from `showWorkflowElement` / the `cmd:fapi-output` launcher) computed via `runToCompletion(config)` — a **fresh run on the static `config.sampleRows` + default inputs**, ignoring the uploaded file and the shared `runEditsAtom` (e.g. FX). So it showed the 25k sample @ FX 1.35 while the run/worksheet showed the real 147,322 @ 1.3978. Now it reads `uploadedRowsAtom` + `runEditsAtom` and computes with the **same `runTemplateCore(config, {rows, overrides, inputs})` call the worksheet uses** → identical numbers by construction (source rows too). (2) **Rate shown as money** — the shared `num()` formatter is 2dp, so `FX_RATE` 1.3978 rendered as **"1.40"** in the run-result step AND the element card (the CAD math already used 1.3978). New `numRate` (full precision) + `isRateKey`/`fig` render rate keys (`/_RATE$|^FX/i`) at rate precision, matching the worksheet's `fmtRate`. (3) **AI readable was integer-rounded** — `activeRun.data.lines/summary` did `Math.round(value)`, which turned `FX_RATE` into **"1"** and dropped cents; now it passes the engine's real precision through. Verified: element-card path === worksheet path (147,322.00 / GROSS_CAD 205,926.69 @ 1.3978); FX displays 1.3978 everywhere; `tsc` 0. Complements the 2026-07-18 calc-engine rate-precision fix below. See ARCHITECTURE.md / UI.md / FEATURES.md.
- **Assistant grounding — the chat now sees the REAL data, for any workflow `[LIVE]` (2026-07-17):** fixed "the chat answers with values from nowhere" (reported: it said the FX rate was "1" and "No editable field FX_RATE" while the sheet showed 1.3978). Root causes + fixes, all **generic across `WORKFLOW_CONFIGS`**: (1) **3-way FX disconnect** — the chat's inline field wrote a standalone `fieldValuesAtom['fx']` that the engine/worksheet/run never read (they read `runEditsAtom['fapi'].inputs.fxRate`), so an edit in chat changed nothing and the model saw a value nothing computed with. New **`ResourceField.binding {workflowId,inputKey}`** (set `fx → fapi/fxRate`) makes `InlineFieldCard` read/write the SAME engine input — one value across chat, sheet, run. (2) **No value grounding** — `use-assistant.tsx` now publishes two always-on readables: **editable-field-values** (every field + current value + `isDefault`; bound fields resolve to the engine input) and **live-workflow-data** (for any workflow with an uploaded source / edited input / active run, the same `createTemplateIntel(cfg).describe()` snapshot the sheet renders — real lines/summary/FX/classification even with **no worksheet open**). (3) **id hallucination** — new **`resolveFieldId()`** fuzzy-resolves the model's guesses (`FX_RATE`/`fx rate`/`fxRate` → `fx`); `editField` uses it and returns the field list on a true miss. (4) `explainWorksheetLine`/`whyWorksheetValue`/`searchWorksheet` gained a **computed-intel fallback** (`resolveIntelFor`) so they answer from the bare chat (no sheet mounted). (5) INSTRUCTIONS gained a **GROUNDING** section (answer values from the readables, say "not set" on `isDefault`, never invent, exact ids). (6) **Trace instrumentation** — `lib/copilot-trace.ts` + a `.use()` hook in `app/api/copilotkit/route.ts` dump the EXACT model input (system + readables + tools + messages) to `.copilot-trace/` when `COPILOT_TRACE=1` — "where did the AI get that?" is now inspectable per turn. Verified: `tsc` 0; `scripts/preview-copilot-context.ts` prints the real 1.3978 snapshot + `resolveFieldId("FX_RATE")→"fx"` against the live functions; the trace helper writes the input; app hot-reloads clean on the running dev server. See ARCHITECTURE.md / UI.md / FEATURES.md / TYPES.md.
- **FX rate precision — accurate until the final results `[LIVE]` (2026-07-18):** follow-up to the grounding fix. The calc engine (`backend/blocks/logic/calculation-engine/run.ts`) applied `roundMoney` (2dp cents) to **every** rule result, including the `FX_RATE` rate — so a rate like 1.3978 was flattened to **1.40** *and then every `*_CAD` conversion multiplied by that rounded rate* (GROSS_CAD used 1.40, not 1.3978). Money-to-cents is right; a **rate is not money**. New `roundResult(value, resultKey)`: rate results (`/_RATE$|^FX/i`, matching the worksheet-intel `isRateKey` convention) flow through at **full precision**; money still rounds to 2dp at each result; final DISPLAY rounding stays at the surface. Now the FX rate stays exact through the whole pipeline and is only presented rounded. Generic — fixes FAPI, Roulement, expense, and the demo (all have `FX_RATE` + `*_CAD`). Verified: fxRate 1.3978 → FX_RATE **1.3978**, GROSS_CAD **34,945.00** (= 25000×1.3978), NET_FAPI_CAD **34,679.42**; and **no regression** at the documented FX 1.35 (NET_FAPI_CAD **33,493.50**, unchanged). `tsc` 0. The field-value and the snapshot FX now **agree**. See ARCHITECTURE.md → "FX / rate precision".
- **Agents made visible + assignable `[LIVE]` (2026-07-17):** the chat now shows WHO did each thing and lets you assign work. (1) **Per-message avatars** — `AsideAssistantMessage` shows the responsible coworker's avatar (`coworkerForMessage()` maps the message's tool call → UI Composer / the workflow specialist / UI Concierge / Workspace Assistant) instead of the generic orb; shared `CoworkerAvatar` (components/assistant/coworker-avatar.tsx). (2) **AgentRoster** (components/assistant/agent-roster.tsx) — an avatar row of the persona agents in the shared thread header (Sofi/Théo/Mira/Nova solid, Rémy dimmed), click → deterministic `launchStartAgent`. (3) **@-mention** — agents added to `composerCommands` so `@sofi`+Enter assigns her (no LLM routing). Additive; tsc 0; verified live (`e2e/deep-chat.spec.ts` 5/5 + screenshots). Details in [UI.md](./UI.md) / [FEATURES.md](./FEATURES.md).
- **Conversation-first "Work" console — increment 1 `[LIVE]` (2026-07-17):** the chat now RECORDS everything it does into a durable, reopenable **Work menu** in the thread header (the "conversation records the work, the Work menu organizes it" direction). New `lib/work-store.ts` — a persisted `atomWithStorage` registry of WorkItems (type / title / status / responsible-coworker / latest-update / open-target) upserted at every choke point: workflow runs (live status running→needs-you→done, from the run effect), worksheets/pages opened, source reviews summoned, generated views — beside the existing `pushTrail`. `components/assistant/work-menu.tsx` (`WorkMenu`) renders the menu (coworker avatar + type + status dot + latest update; **Open** reopens the item's current state, **Jump** scrolls to where it first appeared). Anchoring: rendered cards carry `data-work-id`, message rows carry `data-message-id`, and `jumpToWork()` reuses the `cwp-anchor-flash` scroll-and-flash. Purely **ADDITIVE** — no layout change (the conversation-first flip + scrollbar markers are deferred increments 2/5), nothing serialized into CopilotKit message state, the server-side orphan fix untouched. Stale persisted 'running'/'awaiting' items reconcile to neutral on load. **Deep browser test — Playwright `e2e/deep-chat.spec.ts` PASSES 3/3** against the live chat: renders with the Work menu + no page errors; a sidebar run records the item + shows the coworker (*Sofi · FAPI specialist*) + Jump reaches the card; and an LLM message **+ follow-up both respond with no "Tool result is missing" orphan error** (confirms the can't-chat-after-a-workflow fix live). tsc 0; `next build --turbopack` compiles; adversarial diff review SOUND. **Follow-up (same day): unified the thread scroll** — a launched run now renders INLINE in the single message scroll via a custom CopilotChat `Messages` slot (`components/assistant/thread-messages.tsx` `ThreadMessages` + `PinnedThreadContext`) instead of a separate 70%-tall pane above the chat, so it's one scrollable conversation with the composer fixed at the bottom (fixes the "two chats" feel); verified live incl. a viewport screenshot. Details in [UI.md](./UI.md) / [FEATURES.md](./FEATURES.md).
- **"Can't chat after a workflow" FIXED at the server — orphaned-tool-call repair `[LIVE]` (2026-07-17):** the definitive fix for the follow-up-after-workflow freeze, moving orphan safety from the (silently-failing) client band-aid to the runtime boundary. Confirmed by a 4-agent source audit: `@copilotkit/runtime@1.62.3` drives a default `BuiltInAgent` that hands the whole thread to its bundled **ai@6.0.225** `convertToLanguageModelPrompt`, which throws `MissingToolResultsError` at any user/system boundary (or end-of-thread) with an unpaired assistant tool-call. Orphans arise when the stream emitting a tool call is aborted (Stop / navigation / unmount) before the client handler returns — common right after a workflow, when the user navigates to inspect results. The prior client heal **never landed for workflows**: `agent.setMessages()` deep-clones via `structuredClone`, which throws `DataCloneError` on the `generativeUI` render closure a workflow's assistant message always carries (swallowed → thread stays poisoned). **Fix:** `app/api/copilotkit/route.ts` registers an explicit default `BuiltInAgent` (from `@copilotkit/runtime/v2`, same model driver) with a `.use()` middleware `repairOrphanToolCalls(input.messages)` (new `lib/copilot-orphan-repair.ts`) that injects a synthetic `{role:'tool', toolCallId, content}` for every still-pending call before each user/system boundary — mirroring the SDK's own walk, deterministically, for orphans from ANY source, with no client race/clone hazard and zero impact on client `useCopilotAction` tools (they arrive as `input.tools`, untouched). Verified: tsc 0; a **7/7 unit test** against a faithful ai@6 pairing mirror (every real orphan case would throw → repaired never throws, incl. out-of-order); headless runtime-construction smoke; **4 adversarial source reviews** (3 design + 1 on the actual diff, all sound); and **`next build --turbopack` → "✓ Compiled successfully"**. **Required config:** importing `@copilotkit/runtime/v2` drags in Express (dynamic `require` Turbopack can't bundle), so `next.config.ts` adds `serverExternalPackages: ['@copilotkit/runtime', 'express']` (this route runs in Node) — don't remove it. The client band-aid (`lib/copilot-recovery.ts` + `<CopilotRecovery/>`) has now been **removed** — both files deleted and the `onError` / `onSubmitMessage` / `say()` pre-send guards unwired (app-shell, assistant-thread, use-assistant); the server-side repair is the sole orphan-safety mechanism. See ARCHITECTURE.md → "Chat runtime: server-side orphan repair".
- **Product kernel scaffolded — kernel-migration Step 2 `[LIVE]` (2026-07-17):** `lib/kernel/` now holds the single canonical vocabulary as **pure types + pure helpers (no React/jotai), zero behavior change** — the strangler-fig target the runtimes fold onto next. Modules: `scope` (`Scope` + `ActorKind`/`Actor` — **now the single source of truth**, re-exported by `lib/coworkers.ts`), `evidence` (re-exports the existing Evidence/Governance types verbatim), `workflow` (the 5 canonical node kinds + 4 executors + ONE `WorkflowRun`/`NodeRun`/`Checkpoint`/`Runner`, re-exporting the domain `WorkflowDefinition`), `audit` (`AuditEvent`), `gateway` (`ActionRequest`/`Dispatch`), `agent` (`AgentDefinition`/`AgentRun` + the `ToolInvocation` no-orphan invariant — the typed home for the **"can't follow up after a workflow"** bug, whose structural fix is Step 6), `artifact`, `surface`, `capability`; barrel `lib/kernel/index.ts`. Nothing imports it yet except `coworkers` (`ActorKind`). tsc green + a barrel smoke-import verified. Details in [TYPES.md](./TYPES.md) → "Product Kernel".
- **Coworkers made visible — live activity indicator `[LIVE]` (2026-07-17):** the chat now **names the coworker currently working**. `components/assistant/coworker-activity.tsx` (`CoworkerActivity`, atop `AssistantThread` → shows in both Scope focus mode and the docked panel everywhere) reads a new `activeCoworkerAtom` and renders avatar + `name · role` + an honest **actor tag** (Agent / **Workflow · Deterministic** / Tool / …) + live status + a working pulse. During a run it shows the owning specialist (e.g. *Sofi · FAPI Specialist*) while awaiting a decision and the **Workflow Engine · Deterministic** while computing — the deterministic engine is never mislabeled as an agent (the original "AI coworkers" vision). New `lib/coworkers.ts` (business-role roster from `lib/agents.ts` + fixed Workspace Assistant / UI Concierge / UI Composer / You / System, each with an `ActorKind`); `activeCoworkerAtom`/`setActiveCoworkerAtom` + an optional `actor` on trail events in `lib/workspace-store.ts`; set live by `WorkflowRunFlow`. Supersedes the plain "workflow running" pill. tsc green; `/` serves 200. Details in [UI.md](./UI.md) / [FEATURES.md](./FEATURES.md).
- **Type-system stabilized — kernel-migration Step 1 `[LIVE]` (2026-07-17):** `npx tsc --noEmit` now passes **0 errors** (was 130), all taxonomy drift. **Green-first / zero-behavior:** reconciled `BlockSubtype` with the ~22 subtypes the tool registry already uses, made `"Protected"` a *legal* `BlockFamily` via an internal superset (public 7-family palette unchanged), added `"field"` to `ToolGroup` (both copies) + the `initiates` edge label, filled `Protected`/`Trigger` keys in exhaustive family-maps, and isolated legacy `tax-ui/` from tsc. Also fixed a real runtime bug uncovered en route — a dead lucide `Map` import shadowed the global `Map`, breaking `getMissingIntegrations()`. The spec's deeper removals (Protected-as-family → governance; drop `Field`) are **deferred** to the kernel node-model step (touch connection-validation + the FAPI worksheet, not needed for green). Details in [TYPES.md](./TYPES.md).
- **Two NON-fiscal demo workflows added (2026-07-17) — proves the builder/run/chat spine is domain-agnostic.** Both are full `TemplateConfig`s registered in `lib/workflow-runs/index.ts` (so they run in the chat), have `createX...Workflow()` builders in `local-fiscal-workflow.ts` (so they open in the visual builder), and appear in the launcher (`lib/agents.ts` `WORKFLOWS` + agents **Mira**/**Nova**). (1) **Employee Expense Reimbursement** (`expense`) — 4 source types, classify receipts → policy caps → net payable + CAD; **HAS its own worksheet** (`components/worksheet/expense-worksheet.tsx`, registered page `expense` in `resource-registry.tsx`). (2) **Marketing Campaign Budget Allocation** (`campaign`) — classify channel requests → **human election** of the approved budget between floor/ceiling → allocate + project; **NO worksheet**. Both use the Roulement-style `computeExtra` path (the `source.fapi_inputs` block emits a FIXED key whitelist, so arbitrary non-fiscal params live in `config.params` + editable inputs, not the block). Enabling changes: the elect blocker wording is now **config-driven** (`ElectConfig.floorLabel/ceilingLabel/ceilingWord/floorNote/ceilingNote/promptSuffix`, defaults reproduce Roulement verbatim, `engine.ts`), and `app/builder/page.tsx` now resolves **any** workflow's canvas via `getWorkflowConfig(id).buildSnapshot()` (was a hardcoded fapi/roulement ternary). Verified end-to-end through the real engine (expense net 2,325 USD/3,138.75 CAD; campaign projected 525k @floor / 1,050k @cap); no FAPI/Roulement regression.
- **"Tool result is missing" — SOURCE-AUDITED root cause + definitive fix (2026-07-16, 5-agent audit):** the error is thrown **server-side by the Vercel AI SDK** (`ai@6`), not by CopilotKit/AG-UI. With no explicit `agents`, `CopilotRuntime` drives the model through a **`BuiltInAgent` → `streamText` → `convertToLanguageModelPrompt`**, which walks the WHOLE persisted thread every run and throws **`MissingToolResultsError`** the instant it reaches a user/system message while any assistant tool-call id is unpaired. So ONE orphaned tool call poisons every following message. **The invariant:** every assistant tool-call must be answered by a following `{role:"tool", toolCallId}` before the next user turn. **Two earlier "fixes" were dead code:** (a) `disableParallelToolCalls` only applies in `OpenAIAdapter.process()`, which the BuiltInAgent path never calls (removed as inert); (b) the `onError` recovery never fired — `isOrphanToolError` did `JSON.stringify(err)`, which throws on the error event's circular `copilotkit` ref → caught → false. **Definitive fix — FILL, don't prune:** `lib/copilot-recovery.ts` `fillOrphans(messages)` INJECTS a synthetic `{role:"tool", toolCallId, content}` for any unpaired call (never deletes → no flash-vanish), reading the LIVE `agent.messages` (via `useCopilotChatInternal`, which exposes it directly, so real results are preserved and only genuine orphans fill). `emitOrphanRecovery()` runs it as a **pre-send guard** before every send — composer (`CopilotChat onSubmitMessage`) and programmatic (`say()`) — so the AI SDK never sees an orphan regardless of source (nav-abort, Stop, hot-reload, raw-splice-not-synced). `<CopilotRecovery/>` (single, always-mounted, holds the only recovery `useCopilotChatInternal` to avoid multiplying the connect/detach that aborts runs) is the reactive backstop via the now-working `onError`. Verified: `isOrphanToolError` true on the circular event; `fillOrphans` injects in-position and no-ops when paired. **⚠ SUPERSEDED (2026-07-17):** this client-side heal turned out to fail *specifically for workflows* — `agent.setMessages()` structuredClone-throws on the workflow message's `generativeUI` render closure (swallowed), so the fill never landed. Replaced by the **server-side** orphan repair (see the 2026-07-17 bullet at the top of this log); this client band-aid is now redundant.
- **OpenUI in the chat — `renderUI` bridge (2026-07-16):** the assistant gained a **`generateUI`** CopilotKit action that renders **free-form generated UI inline in the conversation** (not just the `/genui-lab` demo). Its render mounts `components/assistant/genui-render.tsx` (`GenUIRender`) which streams `/api/genui` into OpenUI's `<Renderer>`; lazy `next/dynamic({ssr:false})` keeps the OpenUI chunk off the RSC/route build, and the render waits for `status==='complete'` so it fetches on the full prompt. Routed via an INSTRUCTIONS rule ("generate/create a dashboard|chart|table|form → generateUI") and the `@`-menu GenUI commands. This is the step that lets **one chat do both** — exact worksheet elements (editField/showWorkflowElement) *and* quick generated views. Remaining: writeback from generated forms, real (non-sample) data.
- **`@`-command menu in the composer (2026-07-16):** the assistant composer gained a **pick-by-name command menu** (type `@`) so a capability can be invoked without knowing the tool vocabulary. `use-assistant` exposes a `composerCommands` catalog (open worksheet · edit-fx inline · show FAPI source/output · run FAPI/roulement · generate-UI presets), provided through `AsideComposerContext.commands`; `AsideInput` switches its palette to command-mode on `@`. GenUI presets route to `/genui-lab?p=<prompt>` (the lab auto-runs the preset). Same day: the **GenUI generator got a reliability pass** — default model `gpt-4.1` (`GENUI_MODEL`) + first-line prose-sniff with one hard-steer retry.
- **Chat-as-layer — ambient AssistantPanel (2026-07-15):** the assistant moved from *a place you navigate to* to *a layer that follows you*. The **`ActionOrb` now toggles** a docked **`AssistantPanel`** (`components/assistant/assistant-panel.tsx`) that overlays every route except `/`; `/` stays a full-screen **focus mode** (`ChatWorkspace`). Both surfaces share one brain — **`components/assistant/use-assistant.tsx`** (registers all CopilotKit tools + readables, owns conversation-launch + composer + launcher state) — and one presentational **`components/assistant/assistant-thread.tsx`** (`variant: 'focus' | 'docked'`). The panel returns null on `/`, so `useAssistant()`/the CopilotKit actions **register exactly once** (verified via Playwright). The thread **persists across navigation** because `<CopilotKit>` sits at the `app-shell` root above both; the panel stays mounted while closed (slid off-screen) so context keeps flowing. New atom `assistantOpenAtom` (`lib/chat-store.ts`).
- **Phase 4 — assistant awareness (2026-07-15):** the assistant now knows **where it is** and **what's selected**. A **global page-context readable** (`use-assistant.tsx` → `describeRoute(pathname)`) reports the current route + label on every page. **`components/assistant/builder-copilot.tsx`** (headless, mounted by `app/builder/page.tsx`) publishes the open workflow + the selected block ("the thing clicked on", config summarized) and adds actions **`focusBlock`** (select + scroll/center/highlight) and **`editBlockConfig`** (edit a block's config/label via the same `updateNodeDataAtom` path as the inspector). This is the per-page instrumentation pattern; worksheets/rules editors are next, along with real-route deep-links instead of in-panel tabs.
- **Worksheet intelligence — GENERIC (2026-07-16, Phase 1 FAPI-only → Phase 2 generalized):** the chat answers deep questions about **any template-driven worksheet** — every line value, the exact builder formula, the operands behind it, the classified source rows, and *why* a figure is what it is — **without pasting the whole sheet**. Design = layered retrieval, not vector RAG (numbers are computed, not documents). **`lib/worksheet-intel/`** is the module: `types.ts` (the `WorksheetIntel` contract), `template-adapter.ts` (`createTemplateIntel(config,{rows,inputs})` builds a full intel for ANY `TemplateConfig` off `runTemplateCore` + the config's calc rules — handles FAPI's `formulaExpression` rules AND Roulement's `operands`/`computeExtra` rules), and `registry.ts` (id → live intel). **`components/assistant/worksheet-copilot.tsx`** — generic `<WorksheetCopilot config rows inputs/>` mounted inside a worksheet; publishes one ambient snapshot readable + registers its live intel. The three retrieval actions (`explainWorksheetLine` · `whyWorksheetValue` · `searchWorksheet`) are registered **once, globally** in `use-assistant.tsx` and dispatch through the registry by an optional `worksheet` id (so N worksheets can be open at once). **Proven on both FAPI and Roulement with zero per-worksheet code.** Next: bespoke worksheets (T1134/Surplus/Exec-Overview — static React, not engine-backed) each supply a thin adapter implementing `WorksheetIntel`; an always-on variant (answers when the sheet is closed); optionally an Orama/pgvector semantic index once a fuzzy-language corpus exists.
- **CopilotKit migration (2026-07-13):** the whole app is wrapped in `<CopilotKit runtimeUrl="/api/copilotkit">`; the chat is **`components/workspace/copilot-workspace-panel.tsx`** (CopilotKit `<CopilotChat>` themed monochrome/Attio-style + pages-in-tabs). Tools via `useCopilotAction` (openPage · focusAnchor · editField · closePage · closeAll · openWorkflowBuilder · **runWorkflow** · **showWorkflowElement**); `useCopilotReadable` exposes open pages **+ the active run** (see workflow section).
- **Superseded custom-chat cluster removed (2026-07-13):** the pre-CopilotKit implementation is deleted — `chat-workspace-panel.tsx`, `entity-text.tsx`, `lib/chat-agent.ts`, `app/api/chat-workspace/route.ts`, `lib/fapi-run.ts`, and the old `FapiRunFlow`/`fapi-run-flow.tsx` (its `InlineFieldCard` moved to `components/workspace/inline-field-card.tsx`). `lib/chat-store.ts` slimmed to `chatPageContextAtom` + `chatWorkspaceOpenAtom`. The FAPI/Roulement runs are the pluggable `lib/workflow-runs/` engine.
- **Chat launcher + agents (2026-07-13):** the CopilotKit chat has a **launcher** — **search** (worksheets/fields/workflows/agents; field pins inline, page opens a tab, workflow/agent starts it) plus **Workflow**/**Agent** suggestion chips (`lib/agents.ts`). **Sofi** (FAPI specialist) runs the FAPI flow.
- **Builder-side focus / chat→builder deep-link (2026-07-13):** "Open in builder" on a summoned element (or the LLM) sets `builderFocusTargetAtom = { workflowId, blockId }` then navigates to `/builder`. The builder page (`app/builder/page.tsx`) reads it at mount, loads *that* workflow's template (FAPI/Roulement) as a transient view (not persisted over the saved workflow), selects the target block, and sets `focusNodeIdAtom`. `WorkflowCanvas` watches `focusNodeIdAtom` and `fitView({ nodes:[{id}] })` to **scroll to + center + highlight** that exact block. Target cleared ~1.2s after mount (survives StrictMode). Element→block: source → `sourceBlockId`, output → `summaryBlockId`.
- **Workflow ↔ chat connection (2026-07-13):** the LLM is **run-aware** — `WorkflowRunFlow` publishes to `activeRunAtom` (workflow, document, step index/label, phase, what it's awaiting, headline) and the panel exposes it via `useCopilotReadable`, so the assistant can say where we are / which document / next step. You can **summon workflow elements** into the chat without opening the builder: launcher search ("<workflow> source/output") + the `showWorkflowElement(workflowId, element)` action render `<WorkflowElementCard>` (source = the document rows via `config.sampleRows`; output = `runToCompletion(config)` summary). Each element card has **Open worksheet** + **Open in builder**; the `openWorkflowBuilder` action still opens the full canvas.
- **FAPI mapping ported from Platform (2026-07-13):** Sofi's FAPI classification now uses Platform's authoritative vocabulary. `lib/workflow-runs/fapi-concept-cards.json` is a copy of Platform's `backend/config/fapi-concept-cards.json`; `fapi-mapping.ts` transforms each concept card → a keyword-mapper rule (positive+French indicators → `keywords`, negative+French → `excludeKeywords`, `lineId`/`fieldName` → targets). `TemplateConfig.mapperRules` (set on FAPI) replaces the template's 18 static demo rules at run time; user overrides still take priority. 10 categories, ~11 pos / ~11 neg indicators each. Verified the negative indicators disqualify (e.g. "interest expense" no longer maps to interest income). *(This ports the mapping stage; the calc engine parity swap to `lib/fapi/calculation-engine.ts` — RTF snapping, 95(2) P-coefficients, per-line CAD — is still a separate follow-up.)*
- **Pluggable workflow-run framework (2026-07-13):** `lib/workflow-runs/` — a generic config-driven engine. `runTemplateLoop(config, state)` runs any "classify → roll up → compute" template through the real `runLocalWorkflowTools`, extracts figures + traceability detail, and drives the `upload → categorize → [elect] → approve` loop. **Adding a workflow = one `TemplateConfig`** (`engine.ts` + registry `index.ts`). Live configs: **FAPI** (`fapi.ts`, agent **Sofi**, headline GROSS) and **Roulement fiscal art. 85** (`roulement.ts`, agent **Théo**, headline *Gain différé*). The generic `<WorkflowRunFlow>` (`components/workspace/workflow-run-flow.tsx`) renders any config; the chat `runWorkflow(workflowId)` action + the launcher both use it. Roulement adds an **elect** intervention (choose the art. 85 amount between the PBR floor and JVM ceiling) and a `computeExtra` hook (the template's params source is under-wired, so the deterministic election math is computed in-config from the engine's real PBR total). Verified: FAPI GROSS 29,500 USD · Roulement Gain différé 255,000 USD.
- **Reviewable decision ledger (2026-07-13):** the run is a review surface — **every step is clickable** (clear "Review AI mappings / calculations / result" affordance + hover). The **classify step is interactive** (`ClassifyReview`): it lists every row with the mapper's **AI-suggested category + confidence**, and clicking a category **overrides the AI** (writes `state.overrides` and re-runs the real engine — verified: re-mapping a row moves its amount between buckets and changes the figures). Unmatched rows are flagged amber. Compute step shows the **deterministic** buckets + each line's formula; result step shows every figure traced. Each step (and the FX input) has **"See … in the builder"** — opens `/builder` focused on the exact block that produced it (source → `sourceBlockId`, classify → `mapperBlockId`, aggregation → `rollupBlockId`, calculation → `linesBlockId`, result → `summaryBlockId`, FX input → its currency-rate block) via `builderFocusTargetAtom`/`focusNodeIdAtom`. Also **Open worksheet in a tab**. **Editable inputs** (`EditableInputs` + `config.editableInputs`): an inputs panel in the run lets you change assumptions and the engine recomputes in place (keeping your run position). Two injection paths: **block** (FAPI FX rate → `fapi-source-fx-rate.overrideRate`, inclusion → `fapi-source-inputs.inclusionRate`) and **params** (Roulement JVM / contrepartie / taux → `computeExtra`). Verified: FX 1.35→1.50 lifts NET_FAPI_CAD 33,493.50→34,965; Roulement JVM 500k→700k lifts Gain différé 270k→470k. When a workflow runs it fills the panel (full width, primary height) with a compact chat bar below.
- **FAPI Worksheet rebuilt over the real engine (2026-07-13, Phase 2 of the worksheet redo):** `components/worksheet/fapi-worksheet.tsx` — a new main-app worksheet (Platform's line structure in our monochrome + toned-dot language) that computes from `runTemplateCore(FAPI_CONFIG, { rows, inputs })` over the **shared uploaded rows**, so `/fapi`, the chat's worksheet tab (`resource-registry.tsx` now imports it), the chat run, and the builder canvas all show identical numbers. Sections FX · A · EXP · B · 95(2) · A.1/A.2/C · D–H · FAT + a dual-currency (source/CAD) results summary. Manual lines inline-editable; A/EXP/B expand to their AI-mapped source rows; per-line "trace" opens the builder on that block; Import parses via the shared parser. Dots are **small, pastel, no-glow** (same softened language applied to the run timeline). Verified live: A 22,000 · Gross 25,000 · Net FAPI 24,810 · CAD 33,493.50 at FX 1.35. Replaces the static `tax-ui` worksheet. **Phase 3 (2026-07-13):** a **docked Sources file-viewer** (`SourcesPanel`, toggled from the Sources toolbar) shows the raw trial-balance rows and, on click, each row's provenance (classified category · matched keyword · confidence, or "unmatched") + a trace to the mapper block.
- **Worksheet ⇄ chat parity — stale source rows fixed (2026-07-16):** a second, distinct parity break — the chat run showed the **sample** GROSS (25,000) while the worksheet showed the **imported** file (e.g. 213,922). Both read the shared, persisted `uploadedRowsAtom`, but the run only hydrated it **once at mount** (a `hydratedRef` guard) and its "Use sample workbook" path left stale imported rows in the store — so a file imported via the worksheet's Import button (or imported after the run mounted) never reached the run, and the two surfaces drifted permanently. Fix (`components/workspace/workflow-run-flow.tsx`): the run now reads the store **live** — `effectiveState = storeRows ? { ...state, rows: storeRows, uploaded: true } : state` — so any import anywhere is reflected immediately and the upload step is auto-satisfied when a document already exists; "Use sample workbook" now **clears** the store entry so both surfaces fall back to the sample together. Row resolution on both surfaces is now identically `store[id]?.rows ?? sampleRows`. Verified: same stored rows → worksheet GROSS 216,000 ≡ run headline 216,000 (Net FAPI 215,810).
- **Worksheet ⇄ chat parity bug fixed (2026-07-16):** the FAPI **worksheet** and the **chat run** returned different numbers for the same rows. Cause: lines **C / A.1 / D / E** (`cfaIncome`, `debtForgiveness`, `businessLosses`, `faclCarryforward`) are *classified* from trial-balance rows (rollup named values), and in the calc engine **`fapi_inputs` config wins over the rollup**. The worksheet seeded **every** editable input with its default on mount, injecting `0` into `fapi-source-inputs` and **clobbering those classified lines to 0**; the chat run seeds no inputs until edited, so classification flowed through. Fix (two layers): new `EditableInput.classificationFed` flag (set on those four in `lib/workflow-runs/fapi.ts`) makes `runTemplateCore` **skip injection while the value equals the default** (`engine.ts`), and the worksheet no longer seeds classification-fed keys (`fapi-worksheet.tsx`). Reproduced + verified: a 4,500 CFA-income + 2,000 debt-forgiveness upload gave worksheet GROSS 25,000 / chat 33,500 before; both **33,500** after (Net FAPI 33,310). See ARCHITECTURE.md → "Classification-fed inputs".
- **Full FAPI line set in the builder engine (2026-07-13, Phase 1 of the worksheet redo):** the FAPI workflow now computes the whole Platform line skeleton from **one engine** (`runLocalWorkflowTools`), so the coming worksheet displays real values, not placeholders. Changes: line **A** = `P × (income_bucket − expense_bucket) + 95(2)` (calc engine `formulaExpression`); new display lines **EXPENSES** (`P × expense_bucket`) and **COMPUTATION_95_4** (the 95(2) amount, also flowed into A); **A1/A2/C–H** already had rules and now get fed. The **FAPI Inputs block** (`fapi-source-inputs`) was extended to emit the line-driving assumptions — `pCoefficient (1), canadianRules95_4, debtForgiveness, priorYearG, cfaIncome, businessLosses, faclCarryforward, prescribedAmount, prescribedAmountF1, dividendDeductions, partnershipDividends` — with **RTF snapped to {1.9, 4}**, and all are exposed as **editable inputs** in the run. **95(2) is not a derived formula** — per Platform it's a manual amount added into A; the "P-coefficient" is a scalar income multiplier — so **no new engine operation** was needed and `lib/fapi/calculation-engine.ts` stays unwired (no second calc path). Verified: defaults unchanged (GROSS 25,000 · NET_FAPI 24,810 · CAD 33,493.50); with 95(2)=5,000 + C=10,000 + D=2,000 + F=1,000 → GROSS 40,000 · NET_FAPI 36,810; P=0.5 → A 11,000. *(Phase 2 — the worksheet UI over this line set — is next.)*
- **One source of truth for uploaded data — chat run now uses the real file (2026-07-13):** previously the chat run **ignored the uploaded workbook** and always computed from the 8 built-in `config.sampleRows` (the upload UI even said "parsing not wired"), so the chat and the builder produced different numbers for the "same" file. Fixed: the chat upload now parses the workbook through the **same** parser the builder uses (`lib/workflow-runs/parse-upload.ts` → `parseExcelWorkbookFile` + `buildExcelSourceConfigPatch`), producing `SourceRow[]`. Those rows are stored in a shared, persisted `uploadedRowsAtom` (keyed by workflow id) and the engine (`RunState.rows`, `runTemplateLoop`) computes on them instead of the sample. The builder **hydrates its source block from the same store** when opened via a chat deep-link, so switching chat ↔ builder shows the exact same rows. Verified: sample rows → GROSS 25,000; a different uploaded trial balance → GROSS 21,000 (= A+B on the new rows). "Use sample workbook" clears the store and falls back to `sampleRows`. *(Boundary: a manual upload made **inside** the builder canvas isn't yet mirrored back into the shared store — the store is currently chat-authored.)*
- **Live Bank of Canada FX + API source block (2026-07-13):** the FAPI FX rate is no longer a bare hardcode. A new **API source block** `fapi-api-boc-fx` ("Bank of Canada Valet API", `source:api-http-request`, distinct `Cloud` icon) sits in the FAPI template feeding the Currency Rate block (`fapi-source-fx-rate`), so the **audit lineage shows the real origin** of the rate. The FX input in the run has a **"↻ fetch live rate"** action → `GET /api/fx-rate?from=USD&to=CAD&year=2025` → `fetchAnnualAverageExchangeRate` (real BoC Valet annual-average call, server-side to avoid CORS); the fetched value replaces the input and re-flows the engine, and falls back to the workbook override on any failure. The **currency-rate runner** (`backend/blocks/source/currency-rate/run.ts`) now **prefers `liveRate` over `overrideRate`** (`rate_source: "bank_of_canada_valet"` vs `"override"`; `rate_metadata.live` records which).
- **Inspectable run + chat-always-available (2026-07-13):** `FapiRunFlow` (`fapi-run-flow.tsx`) steps are **expandable** — click a stage to drill into the **source rows**, the **classification** (row → category, matched keyword + confidence), and the **derivations** (buckets + each FAPI line with its formula). The approval step lists **every summary figure with its formula** (`FapiDetail` from the enriched engine) so numbers are traceable before sign-off. **"Open in FAPI worksheet ↗"** opens a page tab (browser-like; the chat conversation persists). The run is **pinned above CopilotChat** (not a full takeover) so chat stays usable, with a **Stop** control to cancel mid-run. *(Sofi's math still uses the builder-block engine; parity swap to `lib/fapi/calculation-engine.ts` pending.)*
- **`AppShell`** (`components/app-shell.tsx`) wraps every route via `layout.tsx`; renders `GlobalTopNav` · `PersistentCanvas` · `ActionOrb` · `GlobalClientSwitcher` inside the **`CopilotKit` provider** (so the chat thread persists across navigation). The chat is no longer rendered here — it's a route.
- **`GlobalTopNav`** (`components/global-top-nav.tsx`) — 52px `#eaeaef` top bar persists across all pages; logo → home; **short + consistent everywhere: `Home · Dashboard · Builder · Scope` links + client selector** (2026-07-14). **The assistant surface (`/`) is branded "Scope" (2026-07-15)** — renamed from "Assistant" across all user-facing labels (navbar, docked panel + focus-mode headers, chat title, `inscope-sidebar`); code identifiers (`AssistantPanel`, `useAssistant`, etc.) keep the "Assistant" name. Each page's own controls live in a per-page **neumorphic sidebar** (`components/neumorphic-sidebar.tsx`), not the navbar. Only `/t1134` still portals into `#global-nav-workflow-slot`.
- **The main page IS the chat (2026-07-14):** **`app/page.tsx` → `ChatWorkspace`** (`components/workspace/copilot-workspace-panel.tsx`) — a full-page **left sidebar (New chat · Workflows · Agents) + conversation + worksheet tabs** on the dark grid. Empty state = **composer centered in the middle**, which crossfades into the conversation on the first message (optimistic, instant — no route change).
- **Page-menu contract + Workflow Builder in the header (2026-07-15):** inline pages publish their menu into the shared panel header (`lib/page-menu-store.ts` `usePageMenu` + `PageMenuBar`) instead of drawing their own toolbar in the narrow body. Step 2 = the **Workflow Builder**: a bridge (`lib/builder-bridge.ts`) lets `WorkflowToolbar` publish Save/Run/Undo/Redo + hide its floating rail when embedded; `builder-page-menu.tsx` puts Edit/View/Workflows/Pages/Settings/Save/Run in the header. Canvas is now clean. **(2026-07-17: Inspector + Preview toggles/panels dropped from the builder — chat + node-click overlay handle inspect/edit/preview; their slot now holds a `Workflows` switcher that loads any built template onto the canvas.)** **Step 2b**: registry pages adopt it via `lib/inline-page-context.tsx` `useInlinePage()` — the **FAPI worksheet** moves Import/Sources/Builder into the header, the **Dashboard** drops its in-body sidebar; the `/fapi` + `/dashboard` routes keep their own chrome (embedded=false).
- **Page ⇄ Chat contract — command + summon any page (2026-07-17):** the general pattern where the chat can **command the page next to it** and **bring any page into the chat**. `lib/page-chat-store.ts` `usePageChat(surface)` lets an open page publish a surface (`context` · `commands` · inline `Embed`) into `pageChatSurfacesAtom` (sibling of `usePageMenu`); `use-assistant.tsx` runs two generic actions over it — **`commandPage`** (acts on the active/adjacent page) and **`bringIntoChat`** (renders a page inline via `SurfaceEmbed`, `components/assistant/surface-embed.tsx`). First adopter: the Workflow Builder (`BuilderCopilot` → context + `checkHealth`/`listBlocks` + a `WorkflowGlance` embed). Any registered page is summonable via the resource-registry fallback. Builds on the per-page-copilot + inline-render primitives already in `use-assistant` (editField/showWorkflowElement/generateUI).
- **Chat fold / expand (2026-07-15):** with a page open, the chat (right) can **fold away** (page full-width + a floating 💬 Chat pill) or **expand** to take over (page tucked behind its tab). `chatPanelModeAtom` (`split`/`collapsed`/`expanded`); controls in a thin chat header.
- **LibreChat dark theme — neumorphic removed (2026-07-15):** the Scope page + assistant were reskinned to a **LibreChat-style dark** look (palette in `lib/librechat-theme.ts`); all neumorphic on the chat surfaces is gone. Messages = user right-bubble / assistant left-with-avatar; composer = dark rounded LibreChat bar; empty state = centered greeting + composer + suggestion chips (the neumorphic hero card + `ScopeLaunchpad` were dropped). **Chat now sits on the RIGHT** when a page is open (`[ sidebar ] · [ page panel ] · [ chat right ]`), fills otherwise. Sidebar is a dark LibreChat rail; `GlobalTopNav` is **hidden on `/`** (flat dark, sidebar owns nav) — keystone navbar stays on other routes. Docked panel darkened to match. Still light for now (styling later): the inline builder's own toolbar + registry pages.
- **LibreChat-style Scope shell — permanent chat + inline pages (2026-07-15):** `ChatWorkspace` rebuilt as **[ left nav sidebar ] · [ permanent chat ] · [ inline page panel ]**. The chat is the always-present main element; opening a page from the sidebar (Workflow Builder · Agent Builder · Dashboard · worksheets) **expands a panel to its right** (LibreChat-artifacts style) with a tab strip — the chat never leaves, so you can ask about the open page (same CopilotKit tree). Pages render inline: `workflow-builder` → `components/workflow/inline-builder.tsx` (the real canvas + `BuilderCopilot`), `agent-builder` → new stub, else the registry component. Opens via `openWorkspaceWindow` (`lib/workspace-store.ts`); all windows stay mounted (state survives tab switches). Styling deferred (neumorphic pass later); `/builder` + `/dashboard` routes still exist as full-page fallbacks. Verified end-to-end.
- **Homepage folded into Scope — one page (2026-07-15):** the separate `/home` page was **removed** (redirects to `/`; `scope-home.tsx` deleted, `Home` nav link removed) — the home→Scope transition felt wrong, so the elements now live on **one page**. Scope's empty state (`assistant-thread.tsx` focus hero) renders the chat composer **plus** `components/assistant/scope-launchpad.tsx`: tiles **Build** (→`/builder`) · **Dashboard** (→`/dashboard`) · **Run FAPI/Roulement** (launch the run **in the chat**) and the **Recent Activity** + **Needs you** cards (direct nav). Light panels floating on the **dark grid** (grid respected). Chat-first, still not AI-only. *(Supersedes the standalone `/home` + its light-page background from earlier the same day.)* A **light neumorphic "start something new" home**: greeting + a **Scope composer** (the product's dot-orb), NL suggestion chips + a "Start fresh: FAPI · Roulement" launcher, and two cards — **Recent Activity** (resume) and **Needs you** (the human-in-the-loop **decision queue** with Approve/Review/Provide items over an ambient running/completed/blocked strip). **Scope = the product** (orb); Sofi/Théo = specialists a run hands to. **AI-first, not AI-only:** every card/launcher does a **direct action** (`router.push` to a real route — worksheet, the standalone `/run/<id>` loop, or `/dashboard`), so the home works with the chat off; the **composer is a resolver** (deterministic navigate for `run …`/`open …`/short page-name queries; everything else escalates to the assistant). Recent/queue data is seed/display for now. Surfaced via the navbar: `GlobalTopNav`'s `NAV_LINKS` leads with **Home** (`Home` icon → `/home`), before Dashboard · Builder · **Scope** (the assistant, `/`).
- **Standalone run route `/run/[workflowId]` (2026-07-15):** the interactive run loop (upload → categorize → [elect] → approve) reachable **without any AI** — `app/run/[workflowId]/page.tsx` renders the same `WorkflowRunFlow` the assistant uses (it's self-contained). The homepage's "Start fresh" launchers point here. This is the linchpin of "usable without the chat."
- **Light-page background (2026-07-15):** `app-shell` paints a full-viewport `#EEF0F5` layer for `isLightPage` (`/home` + `/run/*`) so the floating navbar sits on the page colour instead of a white strip.
- **Scope keystone in the navbar (2026-07-15):** Scope is a **raised "keystone"** (`components/scope-orb.tsx` `ScopeKeystone`) — the bar bulges up in the center into a rounded pedestal (same colour, fused as one silhouette) housing the orb + "SCOPE" label, presiding over the sub-nav (Dashboard · Builder) because Scope encompasses them. Click → `/` (Scope), active-glows purple when there. Nav row grew 52→84px (`CROWN_NAV_H`); bar `min(860px,95vw)` so the flanking groups clear the pedestal. The bottom floating `ActionOrb` was **deleted**; its ambient-panel toggle became a small **"Ask Scope on this page"** button in the navbar (hidden on `/`) — one orb only.
- **Cross-route view transition (2026-07-15):** `lib/view-transition.ts` `viewTransitionNav()` wraps client navigations in `document.startViewTransition()` (the CSS `@view-transition` rule only covers MPA navs), giving a light→dark crossfade between `/home` and Scope (`/`). Wired on all ScopeHome navs + the navbar `NavPill` (both directions). Root fade tuned in `globals.css`; respects `prefers-reduced-motion`. `/chat` redirects to `/`. Page nav (`Assistant · Builder · Dashboard`) moved into `GlobalTopNav`; the old `InScopeHome`/`InScopeSidebar` are unused. The `ActionOrb` is now a single-tap **chat-opener** (→ `/`) shown on every page except the chat. `chatWorkspaceOpenAtom` is unused. *(Earlier: the modal overlay, then the `/chat` route; before that `ChatDrawer`/`ChatCenterOverlay` were removed 2026-07-10.)*
- **`CanvasEnterOverlay`** (inline in `app-shell.tsx`) — `fixed inset-0 z-[15]` overlay; renders `#eaeaef` → transparent (900ms) on every canvas page entry, covering the z-10 content layer so the transition is always visible to the user
- **`PersistentCanvas`** — fixed canvas layer at z-0; bg transitions `#eaeaef` → `#18181c` (900ms); active only on `/builder` and `/workflows/*`
- **Workflow canvas background:** dark `#18181c` with subtle white line grid (`BackgroundVariant.Lines`); fades from `#eaeaef` on mount; `AmbientOrbs` removed
- **Node design:** all 7 family shapes use dark `#25252f` cards on `#18181c` canvas — unified `rgba(255,255,255,0.09)` border, elevation shadow only (no white glow); only icon color unique per family (-400 shade Tailwind)
- **`GlobalClientSwitcher`** (`components/global-client-switcher.tsx`) — global overlay replacing OrbitalStage-local ClientSwitcher
- **`WorkflowToolbar` / `LocalStudioTopBar`** — content portaled into `GlobalTopNav` slot via `createPortal`
- **New stores:** `lib/nav-store.ts` (client + nav actions) · `lib/chat-store.ts` (chat state: `chatWorkspaceOpenAtom`, messages, page context)

### Active development areas (branch: Sampledata)
Based on modified files in current branch:
- `backend/blocks/logic/category-rollup-aggregator/run.ts` — rollup aggregator execution
- `backend/blocks/logic/keyword-mapper/run.ts` — keyword mapper execution updates
- `components/ai-elements/edge.tsx` — edge animation changes
- `components/overlays/configuration-overlay.tsx` — configuration overlay updates
- `components/workflow/config/action-grid.tsx` — action grid UI
- `components/workflow/config/fiscal-block-config.tsx` — fiscal block configuration
- `components/workflow/inspector/block-inspector.tsx` — inspector panel changes
- `components/workflow/inspector/local-ai-panel.tsx` — AI panel in inspector
- `components/workflow/nodes/family-node-shape.tsx` — node visual styling
- `components/workflow/source-viewers/keyword-rulebook-editor.tsx` — keyword rules editor
- `components/workflow/workflow-studio-shell.tsx` — shell restructuring
- `components/workflow/workflow-toolbar.tsx` — toolbar updates
- `components/workflow/workspace/block-data-flow-pane.tsx` — data flow pane
- `lib/local-fiscal-workflow.ts` — workflow serialization
- `lib/local-tool-registry.ts` — tool registry updates
- `lib/local-tool-runner.ts` — runner updates
- `lib/workflow-store.ts` — state atom changes
- `src/domain/workflow/block-catalog.ts` — catalog updates
- `src/domain/workflow/block-types.ts` — type additions
- `src/domain/workflow/workflow-rules.ts` — rule changes

**New untracked files:**
- `components/workflow/logic-viewers/aggregator-workspace.tsx`
- `components/workflow/logic-viewers/calculation-engine-editor.tsx`
- `components/workflow/logic-viewers/calculation-engine-workspace.tsx`
- `components/workflow/logic-viewers/field-block-workspace.tsx`
- `components/workflow/logic-viewers/keyword-mapper-workspace.tsx`
- `components/workflow/source-viewers/rollup-rulebook-editor.tsx`
- `components/workflow/two-panel-tool-shell.tsx`
- `components/workflow/worksheet-page-menu.tsx`
- `components/workflow/worksheet-page-view.tsx`
- `lib/workflow/sample-workflows/fapi-template.ts`

---

## Key File Locations

| What | Where |
|---|---|
| Block families + subtypes | `src/domain/workflow/block-types.ts` |
| Block catalog (10+ templates) | `src/domain/workflow/block-catalog.ts` (domain) + `lib/local-fiscal-workflow.ts` (runtime) |
| Family relationship rules | `src/domain/workflow/workflow-rules.ts` |
| All domain types | `src/domain/workflow/workflow-types.ts` |
| Evidence + governance rules | `src/domain/workflow/source-rules.ts`, `protected-rules.ts` |
| Global state atoms | `lib/workflow-store.ts` |
| Workflow serialization | `lib/local-fiscal-workflow.ts` |
| Tool registry (11 tools) | `lib/local-tool-registry.ts` |
| Workflow execution runner | `lib/local-tool-runner.ts` |
| Backend block `run.ts` files | `backend/blocks/{family}/{subtype}/run.ts` |
| Backend runtime framework | `backend/runtime/` |
| Main shell component | `components/workflow/workflow-studio-shell.tsx` |
| Inspector panel | `components/workflow/inspector/block-inspector.tsx` |
| AI prompt panel | `components/ai-elements/prompt.tsx` |
| Sample workflows | `lib/workflow/sample-workflows/` |
