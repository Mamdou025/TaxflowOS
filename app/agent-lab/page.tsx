// Standalone Agent Lab route. The page component lives in features/ so it can be
// reused both here (bare, full-screen) and embedded as the Agent page's "Lab" tab.
export { default } from '@/features/agent-lab/agent-lab-page';
